# create a empty dictionary .allow 4 friends to entre their fav language as value and use key as their name .assume name are unique 
a = {}
name = input("entre name :")
language = input ("entre lang :")
a.update({name:language})

name = input("entre name :")
language = input ("entre lang :")
a.update({name:language})

name = input("entre name :")
language = input ("entre lang :")
a.update({name:language})

name = input("entre name :")
language = input ("entre lang :")
a.update({name:language})

print(a)

